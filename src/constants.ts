export const GH_INPUT_LIST_TYPES = ['anime', 'manga', 'both'];

export const MAL_LIST_TYPES = ['animelist', 'mangalist'];

export const LIST_STATUS = {
  currently: 1,
  completed: 2,
  on_hold: 3,
  dropped: 4,
  plan_to: 6,
  all: 7,
};

export const FORMAT_TOKEN_PREFIX = '%';

export const DEFAULT_ANIME_FORMAT = '%t - Ep %wep/%tep';

export const DEFAULT_MANGA_FORMAT = '%t - Ch %cr/%tc - %rv/tv';
